language: c
compiler:
  - gcc
  - clang
script: touch * && mkdir install && ./configure --prefix=$(pwd)/install && make install
notifications:
  email:
    recipients:
      - craftik@cyrusian.com
    on_success: always
    on_failure: always
  irc: "irc.ozinger.org#craftIk"
